/**
 * Discovery feed behaviour
 *
 * - `false`: Only real profiles registered on this device (and eventually your backend) appear.
 *   Use for production / new markets with no seed data — users see proper “empty area” states.
 * - `true`: Demo/sample helpers are mixed in so designers and testers always see a list.
 */
export const SHOW_SAMPLE_NEARBY_LISTINGS = false;
