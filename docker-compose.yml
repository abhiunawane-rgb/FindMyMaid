services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: findmymaid
    ports:
      - "5432:5432"
    volumes:
      - findmymaid_pg:/var/lib/postgresql/data

volumes:
  findmymaid_pg:
